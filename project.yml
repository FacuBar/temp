name: AdminApp
options:
  bundleIdPrefix: com.efacubar.admin-app
  deploymentTarget:
    iOS: 16.2
targets:
  AdminApp:
    type: application
    platform: iOS
    sources: 
      - AdminApp
    info:
      path: AdminApp/Info.plist